# Fly.io config for a static site served by Nginx
app = "swasthya-setu"
primary_region = "bom"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
